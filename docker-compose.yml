services:
  fnos-teamdir-utils:
    image: ghcr.io/sqkkyzx/fnos-teamdir-utils
    container_name: fnos-teamdir-utils
    privileged: true
    pid: host
    restart: no
    network_mode: "none"
    environment:
      - DEFAULT_POOL_FOR_PERSONAL_DIR=2
      - CLEAN_MODE=0